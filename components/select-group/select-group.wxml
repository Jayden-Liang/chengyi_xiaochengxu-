<!--components/select-group/select-group.wxml-->
<view class="flexclass">
 <block wx:for="{{typelist}}" wx:key="index">
    <view data-index="{{index}}" bindtap="tapName" class="each {{active[index] == 1 ? 'activeclass' : ''}}">{{item}}</view>
  </block>
</view>